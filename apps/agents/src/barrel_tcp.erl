-module(barrel_tcp).
-export([connect/3, recv/3, start_server/1]).

connect(Host, Port, Options) ->
    missing_function_stubs:barrel_tcp_connect(Host, Port, Options).

recv(Socket, Length, Timeout) ->
    missing_function_stubs:barrel_tcp_recv(Socket, Length, Timeout).

start_server(Options) ->
    missing_function_stubs:barrel_tcp_start_server(Options).